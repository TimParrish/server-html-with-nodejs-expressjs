<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Dynamic HTML With EJS</title>

    <style>
      body {
        padding: 50px;
        background-color: lightgray;
      }
      .header h1 {
        color: darkblue;
        text-align: center;
      }
      .fizzbuzz p {
        text-align: center;
      }
      .fizzbuzz h2 {
        text-align: center;
      }
      .footer p {
        text-align: center;
      }
    </style>
  </head>
  <body>
    <div class="header">
      <h1>FizzBuzz HTML Generator With NodeJS, Express & EJS</h1>
    </div>
    <div class="fizzbuzz">
      <p>
        Pass your FizzBuzz number as a url parameter at the route
        /fizzbuzz/yourNumber
      </p>
      <h2>Your FizzBuzz number: <%= data.userInput %></h2>

      <p>
        <% for( let counter = data.userInput; counter > 0; counter--){
        if(counter %3 == 0){ %>Fizz<% } if(counter %5 == 0){ %>Buzz<% }
        if(counter %5 != 0 && counter %3 != 0){ %> <%= counter %> <% } %>
        <br />
        <% } %>
      </p>
    </div>
    <div class="footer">
      <p>
        View the
        <a
          href="https://www.npmjs.com/package/ejs"
          alt="EJS package for NodeJS documentation"
          >Documentation for the EJS package for NodeJS</a
        >
      </p>
    </div>
  </body>
</html>
